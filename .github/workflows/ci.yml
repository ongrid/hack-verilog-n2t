name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: Run Testbenches
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Cache apt packages
        uses: actions/cache@v4
        id: cache-apt
        with:
          path: ~/apt-cache
          key: ${{ runner.os }}-apt-iverilog-${{ hashFiles('.github/workflows/ci.yml') }}
          restore-keys: |
            ${{ runner.os }}-apt-iverilog-

      - name: Install Icarus Verilog
        run: |
          if [ "${{ steps.cache-apt.outputs.cache-hit }}" != "true" ]; then
            mkdir -p ~/apt-cache
            sudo apt-get update
            sudo apt-get install -y --download-only iverilog
            if ls /var/cache/apt/archives/*.deb 1> /dev/null 2>&1; then
              cp /var/cache/apt/archives/*.deb ~/apt-cache/
            fi
          else
            echo "Using cached apt packages"
            if ls ~/apt-cache/*.deb 1> /dev/null 2>&1; then
              sudo cp ~/apt-cache/*.deb /var/cache/apt/archives/
            fi
          fi
          sudo apt-get install -y iverilog

      - name: Verify installation
        run: |
          iverilog -V
          vvp -V

      - name: Run all testbenches
        run: make sim

      - name: Upload waveforms (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: waveforms
          path: "**/*.vcd"
          retention-days: 7
